---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: apme-abbenay-token
  namespace: apme-dev
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: bitwarden-fields
    kind: ClusterSecretStore
  target:
    name: apme-dev-abbenay
    deletionPolicy: Delete
    template:
      type: Opaque
      data:
        token: "{{ .token }}"
  data:
    - secretKey: token
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: ClusterSecretStore
      remoteRef:
        key: ocp-lab/apme-abbenay
        property: abbenay-token
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: apme-gcp-credentials
  namespace: apme-dev
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: bitwarden-fields
    kind: ClusterSecretStore
  target:
    name: apme-gcp-credentials
    deletionPolicy: Delete
    template:
      type: Opaque
      data:
        service-account-key.json: "{{ .gcpsakey }}"
  data:
    - secretKey: gcpsakey
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: ClusterSecretStore
      remoteRef:
        key: ocp-lab/apme-abbenay
        property: gcp-service-account-key
