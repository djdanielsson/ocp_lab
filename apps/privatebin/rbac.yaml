---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: privatebin-scc-nonroot
  namespace: privatebin
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:openshift:scc:nonroot-v2
subjects:
  - kind: ServiceAccount
    # Use 'privatebin' if the chart creates its own SA, otherwise use 'default'
    name: default 
    namespace: privatebin
