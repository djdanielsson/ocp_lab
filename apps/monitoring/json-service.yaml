apiVersion: v1
kind: Service
metadata:
  name: json-exporter
  namespace: prometheus-operator
  labels:
    app: json-exporter
spec:
  selector:
    app: json-exporter
  ports:
    - name: http
      port: 7979
      targetPort: 7979
