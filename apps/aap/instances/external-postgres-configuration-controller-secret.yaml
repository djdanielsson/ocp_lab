---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: external-postgres-configuration-controller
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: bitwarden-login
    kind: ClusterSecretStore
  target:
    name: external-postgres-configuration-controller
    deletionPolicy: Delete
    template:
      type: Opaque
      data:
        host: "aap-postgres-rw.aap.svc.cluster.local"
        port: "5432"
        database: "controller"
        username: "controller"
        password: "{{ .password }}"
        type: "unmanaged"
  data:
    - secretKey: password
      sourceRef:
        storeRef:
          name: bitwarden-login
          kind: ClusterSecretStore
      remoteRef:
        key: ocp-lab/postgres-controller
        property: password
