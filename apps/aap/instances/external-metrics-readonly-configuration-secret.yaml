---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: external-metrics-readonly-configuration
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: bitwarden-login
    kind: ClusterSecretStore
  target:
    name: external-metrics-readonly-configuration
    deletionPolicy: Delete
    template:
      type: Opaque
      data:
        host: "aap-postgres-rw.aap.svc.cluster.local"
        port: "5432"
        database: "automationcontroller"
        username: "ms_awx_readonly"
        password: "{{ .password }}"
        type: "unmanaged"
  data:
    - secretKey: password
      sourceRef:
        storeRef:
          name: bitwarden-login
          kind: ClusterSecretStore
      remoteRef:
        key: ocp-lab/postgres-metrics-readonly
        property: password
